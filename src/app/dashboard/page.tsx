// src/app/dashboards/page.tsx

import Dashboard from '@/components/Dashboard'; // Importando seu componente já pronto
// O '@/' é um atalho para a pasta 'src/' que o Next.js já configura para você.

export default function DashboardsPage() {
  return (
    <Dashboard />
  );
}